{
  "dependencies": {
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "eslint": "^8.57.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ."
  }
}
